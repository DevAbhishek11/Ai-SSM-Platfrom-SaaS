/**
 * Test stub for Next.js' `server-only` guard package, which throws when a
 * module graph is imported outside a server environment. Vitest runs these
 * modules directly, so the alias in `vitest.config.ts` points here.
 */
export {};
